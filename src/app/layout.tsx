import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { ThemeProvider } from "@/components/theme-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Tender Intelligence — Government Procurement Monitoring Platform",
  description:
    "Enterprise SaaS for monitoring government contracts, tenders, procurement notices, and infrastructure opportunities worldwide. AI-powered summarization, risk assessment, and opportunity scoring.",
  keywords: [
    "tender intelligence",
    "government procurement",
    "public tenders",
    "procurement monitoring",
    "opportunity pipeline",
    "bid management",
  ],
  authors: [{ name: "Tender Intelligence" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="light"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
